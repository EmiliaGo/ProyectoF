<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel = "stylesheet" href="/css/estilo.css">
    <title>Proyecto CRUD con Node.js y MySQL</title>
</head>
<body>
    <h1>Lista de usuarios</h1>
    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Correo</th>
                <th>Teléfono</th>
                <th>Dirección</th>
                <th>Edad</th>
                <th>Fecha de Nacimiento</th>
                <th>Comida Favorita</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <% users.forEach(user => { %>
                <tr>
                    <td><%= user.name %></td>
                    <td><%= user.email %></td>
                    <td><%= user.telefono %></td>
                    <td><%= user.direccion %></td>
                    <td><%= user.edad %></td>
                    <td><%= user.fecha_nacimiento %></td>
                    <td><%= user.comida_fav %></td>
                    <td>
                        <a href="/edit/<%= user.id %>">Editar</a>
                        <a href="/delete/<%= user.id %>">Eliminar</a>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
    
<br>
<a href="/add">Agregar usuario</a>
</body>
</html>